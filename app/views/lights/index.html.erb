<div>
  <% @lights.each do |light| %>
    <div>
      <div>
        <span>Light Name: </span>
        <span><%= light.name ? light.name : "Unnamed" %></span>
      </div>
      <div>
        <span>Group: </span>
        <span><%= light.light_groups ? light.light_groups : "Ungrouped" %></span>
      </div>
    </div>
  <% end %>
</div>